package p129;

import java.util.HashMap;

public class MapTest {

	public static void main(String[] args) {
		HashMap<String, Integer> scores = new HashMap<>();
		
		scores.put(key, value);
		
		scores.remove(key);
		
		scores.size();
		
		scores.containsKey(key)
		
		scores.containsValue(value);
	}

}
